    <h1>{{ferme.nom}}</h1>
<form method="post" action="{{ path('app_delete_ferme', {'id': ferme.id}) }}"
 onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{csrf_token('delete' ~ ferme.id) }}">
    <button class="btn">Delete</button>
</form>